import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

export const appstoreSice = createSlice({
  name: "appstore",
  initialState,
  reducers: {}
});

export const {} = appstoreSice.actions;