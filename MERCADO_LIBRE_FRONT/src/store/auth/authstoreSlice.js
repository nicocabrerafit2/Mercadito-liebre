import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

export const authstoreSlice = createSlice({
    name: 'auth',
    initialState,
    reducers: {}
});

export const { } = authstoreSlice.actions;